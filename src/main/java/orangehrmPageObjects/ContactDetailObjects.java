package orangehrmPageObjects;

import org.openqa.selenium.WebDriver;

public class ContactDetailObjects extends BaseActions {
    WebDriver driver;
    public ContactDetailObjects(WebDriver driver) {
        super(driver);
        this.driver = driver;
    }
    }
